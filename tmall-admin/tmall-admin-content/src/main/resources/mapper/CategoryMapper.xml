<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cn.tedu.tmall.admin.content.dao.persist.mapper.CategoryMapper">

         <!--
           //select id, name, keywords, sort, enable, is_display  from content_category
    List<CategoryListItemVO> list();
         -->
    <select id="list" resultType="cn.tedu.tmall.admin.content.pojo.vo.CategoryListItemVO">
        select id, name, keywords, sort, enable, is_display  from content_category
    </select>

</mapper>
